export type columnsType = {
  data: any,
  number: number;
  paid: string;
  pay: string;
  key: number;
  date: JSX.Element;
  name: JSX.Element;
  stages: JSX.Element | null;
};

export declare type AslignType = 'left' | 'center' | 'right';