export type columnsType = {
  key: string;
  number: number;
  name: string;
  address: string; 
  email: string;
  phone: string;
  clientCategory: string;
};

export declare type AslignType = 'left' | 'center' | 'right';